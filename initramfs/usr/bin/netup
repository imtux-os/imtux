#!/bin/sh

ETH_IFACE="${1:-eth0}"

# Enable the loopback device
ip link set lo up
ip addr add 127.0.0.1/8 dev lo 2>/dev/null

# Enable the ethernet interface
ip link set ${ETH_IFACE} up
udhcpc -i ${ETH_IFACE} -s /etc/udhcpc/default.script
